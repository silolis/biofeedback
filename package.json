{
  "name": "hrv-biofeedback",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "HRV resonance-frequency biofeedback — single-page app + offline analysis tools",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.41.0",
    "vite": "^6.3.5",
    "vite-plugin-singlefile": "^2.2.0",
    "wrangler": "^4.101.0"
  }
}
